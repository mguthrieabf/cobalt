################################
# installs basic support files #
# on each environment.         #
################################
files:
  "/usr/local/bin/tlog":
      mode: "000755"
      content : |
        #!/bin/bash

        tail -100f /var/log/web.stdout.log
